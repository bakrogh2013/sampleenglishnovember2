# Netlify config: route /api/check to the serverless function, set basic headers
[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/check"
  to = "/.netlify/functions/check"
  status = 200
